function concatData(input) {
    let firstName = input;
    let lastName = input;
    let age = Number();
    let concatData = firstName + " " + lastName + "@" + age;
    console.log(concatData);
}

concatData()